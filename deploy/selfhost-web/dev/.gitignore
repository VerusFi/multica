# Throwaway verification-harness artifacts, all regenerated by
# setup-harness.sh / verify-net.mjs / verify-boot.mjs / verify-luks.mjs /
# verify-install.mjs / verify-firstboot.mjs. Only setup-harness.sh,
# harness.html, verify-net.mjs, harness-boot.html, verify-boot.mjs,
# harness-luks.html, verify-luks.mjs, harness-install.html,
# verify-install.mjs, harness-firstboot.html, verify-firstboot.mjs and
# NOTES.md are meant to be committed from this directory.
alpine-virt-3.23.5-x86.iso
/iso/
/bios/
node_modules/
package.json
package-lock.json
empty-disk.img
# Symlink to the locally-built payload tarball (Task 5's
# build-selfhost-tarball.sh output, e.g. /tmp/multica-selfhost-386.tar.gz)
# served by verify-firstboot.mjs's static server for the harness's guest to
# download over sh_release_url — not committed, regenerated by whoever runs
# the Task 8 boot test locally.
multica-selfhost-386.tar.gz
